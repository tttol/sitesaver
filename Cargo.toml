[package]
name = "sitesaver"
version = "0.1.0"
edition = "2024"
[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
reqwest = { version = "0.13.3", features = ["blocking", "charset"] }
scraper = "0.27.0"
